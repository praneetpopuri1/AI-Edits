How does one make a render, You input a series of edits and then the renderer excutes those edits, plan and execute
the input should be template, like Metadata_of_Edit_1: Description_of_Edit_1,Metadata_of_Edit_2: Description_of_Edit_2, ...
where the render executes n edits
this goes on until all of the edits are executed
is there any other way this could happen
that is depedent on the capabilites of the render
if the render needs timestamps of each edit then thats different
One idea of this compared to the old one is that the render can just worry about planning and not creating a valid json or
to go viral music is very important


seeing what prompts the VLM has in the old system







have to make your work engaging or else then your in hell I think i dont know



how does the old model generate test plans and what should we do differently 

1: metadata extration from ffprobe (deterministic)
2: first VLM pass gets temporal event extraction (mm:ss.ff)
3: python validation and normalizing timestamps (determinstic validation)
4: 2nd pass: making edit decisions from validated events
5: deterministic final plan assembly

1:
    for getting metadata there is call to a helper function that 
    this is the command
    cmd = [
            "ffprobe",
            "-v",
            "error",
            "-select_streams",
            "v:0",
            "-show_entries",
            "stream=width,height,r_frame_rate,duration",
            "-of",
            "json",
            path,
        ]
    bassically just formats a .json file with meta data of width,height,r_frame_rate,duration
    example output
    Source metadata:
    {
    "duration_s": 37.302,
    "width": 1920,
    "height": 1080,
    "fps": 30.0
    }
    example output for prompt
    "
    You are analyzing a video for an editing pipeline.

    Source metadata:
    {json.dumps(source_meta, indent=2)}

    Return JSON only with this exact shape:
    {{
    "summary": "short summary",
    "events": [
        {{
        "start": "mm:ss.ff",
        "end": "mm:ss.ff",
        "description": "short visual description",
        "visible_objects": ["object"],
        "confidence": "low|medium|high"
        }}
    ]
    }}

    Rules:
    - Output at most 6 events.
    - Timestamps must be in mm:ss.ff format.
    - Each event must be at least 1 second long.
    - Each description must be under 100 characters.
    - Do NOT read, transcribe, or guess text on objects.
    - Do NOT include product labels, book titles, brands, logos, or long numbers.
    - Do NOT include markdown fences.
    - If unsure, say "person holding object" or "person speaking".
    "

after this the next pass is:



what should the usewr prompts look like, that depends on the desired output:
what do I think the model can do, and what should it do, to optmize for long form edits
I have should have like 3-4 different styles of videos to target, to make edits for 
first being mr beast videos, trash taste, big a videos, and small ant videos (maybe cut game theory videos for a podcast)

takes a 30 second segement and applies n edits to that segment based the video context and 
    
https://www.twitch.tv/videos/2768135852

two types of prompts global prompts and local prompts

global:
    global prompts need to search through the entire video or context length
    user prompt: locate the timestamps where the youtuber is talking about british politics

    user prompt: cut out the parts of the video where the youtuber is not doing anything, or is away from the camera

local:
    
    specific to the current frame the editor is on, does not need global context
    user prompt: zoom into the youtuber for the 5 seconds
    user prompt: overlay a bunch images


more prompts:
    uprompt: generate a video intro 
    uprmpt: generate captions (at the end of editing)
    uprompt: (given context window of drive of files including videos and images) overlay the video of the rhinos from the trip

so different content has different editing requirments 

an AI editing assistant should not try to be greedy and try to do stuff that humans are better suited for, should stick to tasks that A) are boring B) take a while for a human to do  
